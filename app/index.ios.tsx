import { TextIOS } from "@/components/TextIOS";
import { View } from "react-native";

export default function HomeScreen() {
  return (
    <View style={{ paddingHorizontal: 16, paddingVertical: 24 }}>
      <TextIOS/>
    </View>
  );
}
